{
  "scripts": {
    "dev": "sudo npx vite --port 80 --host",
    "build": "sudo npx vite build"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
